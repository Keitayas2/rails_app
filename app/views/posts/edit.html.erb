<%= form_for @post do |f| %>
    <div class="field">
        <%= f.label :body %><br>
        <%= f.text_area :body %>
        <%= f.file_field :image %>
    </div>
    <div class="actions">
        <%= f.submit "編集する"%>
    </div>
<% end %>